import java.util.ArrayList;
import java.util.Arrays;
import java.util.LinkedList;

/**
 * 
 */

/**
 * @author usuario
 *
 */
public class Ejercicio3 {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		ArrayList listaArray=new ArrayList<>();
		LinkedList listaLinked=new LinkedList<>();
		String numerosArray="";
		String numerosLinked="";
		Inetegr comodin=0;
		for (int i = 1; i < 15; i++) {
			listaLinked.add((int) (Math.random()*15)+1);
			numerosLinked=numerosLinked+
			listaArray.add((int) (Math.random()*15)+1);
		}
		Leer.mensaje(listaLinked.toString());
		Leer.mensaje(listaArray.toString());
	}
}
