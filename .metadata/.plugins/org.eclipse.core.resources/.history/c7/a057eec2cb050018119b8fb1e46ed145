import java.util.Scanner;

/**
 * 
 */

/**
 * @author usuario
 *
 */
public class Principal {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Autor autores[]=new Autor[100];
		Libro libros[]=new Libro[100];
		Scanner teclado=new Scanner(System.in);
		int opcion=10;
		
		while (opcion!=0) {
			System.out.println();
			System.out.println("1. Crear libro");
			System.out.println("2. Modificar Autor");
			System.out.println("3. Modificar libro");
			System.out.println("4. Listado de Libros");
			System.out.println("5. Listado de Autores");
			System.out.println("0. Salir");
			opcion=Leer.pedirEntero("");
			menu(opcion, autores, libros);
		}
	}

	private static void menu(int opcion,Autor[] autores, Libro[] libros) {
		switch (opcion) {
		case 1:
			int contador;
			contador=Leer.pedirEntero("Cuantos libros desea crear?");
			while (contador!=0) {
				String nombreLibro=Leer.pedirCadena("Cual es el nombre del libro "+contador);
				for (int i = 0; i < autores.length; i++) {
					
					
					
					
					
					if (autores[i]== null) {
						
						break;
					}
				}
				Double preciolibro=Leer.pedirDecimal("Cual es el precio del libro "+contador);
				Integer cantidadLibro=Leer.pedirEntero("De cuanto es el stock?");
				
				libros[Libro.getSiguiente()-1]=new Libro(nombreLibro, autor, preciolibro, cantidadLibro)
				contador--;
			}
			break;
		case 2:
			
			break;
		case 3:

			break;
		case 4:
			
			break;
		case 5:

			break;

		default:
			break;
		}
	}

}
